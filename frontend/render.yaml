- type: web
  name: aspirant-news-dashboard
  runtime: python
  plan: free
  buildCommand: cd frontend && pip install -r requirements.txt
  startCommand: cd frontend && streamlit run streamlit_app.py --server.port $PORT --server.address 0.0.0.0
  envVars:
    - key: API_URL
      sync: false